apiVersion: v1
kind: Service
metadata:
  name: abap-compiler
  labels:
    app: abap-compiler
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: abap-compiler
  ports:
  - name: http
    port: 8093
    targetPort: 8093
    protocol: TCP
