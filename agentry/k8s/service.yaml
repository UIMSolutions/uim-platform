apiVersion: v1
kind: Service
metadata:
  name: agentry
  namespace: uim-platform
  labels:
    app: agentry
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: agentry
  ports:
  - name: http
    port: 8117
    targetPort: 8117
    protocol: TCP
