apiVersion: apps/v1
kind: Deployment
metadata:
  name: application-autoscaler
  labels:
    app: application-autoscaler
    part-of: uim-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: application-autoscaler
  template:
    metadata:
      labels:
        app: application-autoscaler
    spec:
      containers:
      - name: application-autoscaler
        image: uim-platform/application-autoscaler:latest
        ports:
        - containerPort: 8097
          protocol: TCP
        envFrom:
        - configMapRef:
            name: application-autoscaler-config
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 8097
          initialDelaySeconds: 5
          periodSeconds: 30
          timeoutSeconds: 3
        readinessProbe:
          httpGet:
            path: /api/v1/health
            port: 8097
          initialDelaySeconds: 3
          periodSeconds: 10
          timeoutSeconds: 3
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        securityContext:
          runAsNonRoot: true
          readOnlyRootFilesystem: true
