apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-personal-data
  labels:
    app: cloud-personal-data
    part-of: uim-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloud-personal-data
  template:
    metadata:
      labels:
        app: cloud-personal-data
    spec:
      containers:
      - name: cloud-personal-data
        image: uim-platform/cloud-personal-data:latest
        ports:
        - containerPort: 8102
          protocol: TCP
        envFrom:
        - configMapRef:
            name: cloud-personal-data-config
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 8102
          initialDelaySeconds: 5
          periodSeconds: 30
          timeoutSeconds: 3
        readinessProbe:
          httpGet:
            path: /api/v1/health
            port: 8102
          initialDelaySeconds: 3
          periodSeconds: 10
          timeoutSeconds: 3
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        securityContext:
          runAsNonRoot: true
          readOnlyRootFilesystem: true
