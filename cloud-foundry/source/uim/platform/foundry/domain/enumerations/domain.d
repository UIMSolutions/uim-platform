module uim.platform.foundry.domain.enumerations.domain;

enum MappingStatus {
    active,
    inactive,
    pending,
    error,
}

enum MappingType {
    applicationRoute,
    saasRoute,
    staticRoute,
}