apiVersion: v1
kind: Service
metadata:
  name: task-center
  namespace: uim-platform
  labels:
    app: task-center
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: task-center
  ports:
  - name: http
    port: 8103
    targetPort: 8103
    protocol: TCP
