apiVersion: v1
kind: Service
metadata:
  name: field-service
  namespace: uim-platform
  labels:
    app: field-service
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: field-service
  ports:
  - name: http
    port: 8107
    targetPort: 8107
    protocol: TCP
