apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-mesh
  namespace: uim-platform
  labels:
    app: event-mesh
    part-of: uim-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-mesh
  template:
    metadata:
      labels:
        app: event-mesh
    spec:
      containers:
      - name: event-mesh
        image: uim-platform/event-mesh:latest
        ports:
        - containerPort: 8108
          protocol: TCP
        envFrom:
        - configMapRef:
            name: event-mesh-config
        livenessProbe:
          httpGet:
            path: /health
            port: 8108
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8108
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
