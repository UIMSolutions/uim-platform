apiVersion: v1
kind: ConfigMap
metadata:
  name: identity-config
  namespace: uim-platform
data:
  IDENTITY_HOST: "0.0.0.0"
  IDENTITY_PORT: "8121"
  IDENTITY_BACKEND: "memory"
  IDENTITY_DATA_DIR: "/data/identity"
  IDENTITY_MONGO_DB: "uim_identity"
