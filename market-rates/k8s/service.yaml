apiVersion: v1
kind: Service
metadata:
  name: market-rates
  labels:
    app: market-rates
    tier: platform-service
spec:
  type: ClusterIP
  selector:
    app: market-rates
  ports:
  - name: http
    port: 8097
    targetPort: 8097
    protocol: TCP
