apiVersion: apps/v1
kind: Deployment
metadata:
  name: AiDocument-ai
  labels:
    app: AiDocument-ai
    part-of: uim-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: AiDocument-ai
  template:
    metadata:
      labels:
        app: AiDocument-ai
    spec:
      containers:
      - name: AiDocument-ai
        image: uim-platform/AiDocument-ai:latest
        ports:
        - containerPort: 8096
          protocol: TCP
        envFrom:
        - configMapRef:
            name: AiDocument-ai-config
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 8096
          initialDelaySeconds: 5
          periodSeconds: 30
          timeoutSeconds: 3
        readinessProbe:
          httpGet:
            path: /api/v1/health
            port: 8096
          initialDelaySeconds: 3
          periodSeconds: 10
          timeoutSeconds: 3
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        securityContext:
          runAsNonRoot: true
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
