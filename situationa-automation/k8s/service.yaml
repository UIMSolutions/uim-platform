apiVersion: v1
kind: Service
metadata:
  name: cloud-situation-automation
  labels:
    app: cloud-situation-automation
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: cloud-situation-automation
  ports:
  - name: http
    port: 8100
    targetPort: 8100
    protocol: TCP
