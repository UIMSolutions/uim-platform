apiVersion: v1
kind: Service
metadata:
  name: analytics
  labels:
    app: analytics
spec:
  type: ClusterIP
  selector:
    app: analytics
  ports:
  - name: http
    protocol: TCP
    port: 8112
    targetPort: 8112
