apiVersion: v1
kind: Service
metadata:
  name: credential-store
  labels:
    app: credential-store
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: credential-store
  ports:
  - name: http
    port: 8095
    targetPort: 8095
    protocol: TCP
