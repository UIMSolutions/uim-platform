apiVersion: v1
kind: Service
metadata:
  name: uim-ui-flexibility
  labels:
    app: uim-ui-flexibility
    component: platform-service
spec:
  type: ClusterIP
  selector:
    app: uim-ui-flexibility
  ports:
  - name: http
    protocol: TCP
    port: 8098
    targetPort: 8098
