apiVersion: v1
kind: Service
metadata:
  name: cloud-translation
  labels:
    app: cloud-translation
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: cloud-translation
  ports:
  - name: http
    port: 8096
    targetPort: 8096
    protocol: TCP
