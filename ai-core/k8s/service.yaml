apiVersion: v1
kind: Service
metadata:
  name: ai-core
  labels:
    app: ai-core
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: ai-core
  ports:
  - name: http
    port: 8090
    targetPort: 8090
    protocol: TCP
